@page
@using Telamingo.Domain.Enums;
@using Telamingo.Domain.Dtos;
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}
@*  <section class="pt-3 pb-4" id="count-stats">
    <div class="container">
      <div class="row">
        <div class="col-lg-9 z-index-2 border-radius-xl mt-n10 mx-auto py-3 blur shadow-blur">
          <div class="row">
            <div class="col-md-4 position-relative">
              <div class="p-3 text-center">
                <h1 class="text-gradient text-primary"><span id="state1" countTo="70">0</span>+</h1>
                <h5 class="mt-3">Coded Elements</h5>
                <p class="text-sm">From buttons, to inputs, navbars, alerts or cards, you are covered</p>
              </div>
              <hr class="vertical dark">
            </div>
            <div class="col-md-4 position-relative">
              <div class="p-3 text-center">
                <h1 class="text-gradient text-primary"> <span id="state2" countTo="15">0</span>+</h1>
                <h5 class="mt-3">Design Blocks</h5>
                <p class="text-sm">Mix the sections, change the colors and unleash your creativity</p>
              </div>
              <hr class="vertical dark">
            </div>
            <div class="col-md-4">
              <div class="p-3 text-center">
                <h1 class="text-gradient text-primary" id="state3" countTo="4">0</h1>
                <h5 class="mt-3">Pages</h5>
                <p class="text-sm">Save 3-4 weeks of work when you use our pre-made pages for your website</p>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>
*@
    <div class="col-md-7 mx-auto text-center"
        style=" display: flex;
                flex-direction: row;">
@*        <label style="margin: auto;" for="cars">TargetCuntry:</label>*@
        <select style="width: 50%;" class="form-control" name="mdl.PersonalInformation.TargetCuntry">
            <option value=""> Where do you whant to go?</option>
        @{
            foreach(var item in Enum.GetValues(typeof(EnumTargetCuntry)).OfType<EnumTargetCuntry>().ToArray())
            {
            <option value="@((int)item)"> @item.ToString()</option>
            }
        }
        </select>  
        <a href="./Form">
            <button type="button" class="btn btn-primary btn-lg mb-3 mb-sm-0">Get started</button>
        </a>
    </div>

<style>
    .col-4 {
        flex: 0 0 auto;
        width: 90%;
    }
</style>

@*<section class="pt-lg-7 pt-5">
 <form asp-page-handler="./Calculations" method="post">
    <div class="container">
      <div class="row">
        <div class="col-lg-5 col-md-8 order-2 order-md-2 order-lg-1">
        <div class="p-3 pt-0">
        <div class="icon icon-shape bg-gradient-primary rounded-circle shadow text-center mb-4">
            <i class="ni ni-building"></i>
        </div>
        <h4 class="text-gradient text-primary mb-0">Personal information:</h4>
            <div class="row text-center py-3 mt-3">
                <div class="col-4 mx-auto">
                <input type="text" placeholder="FirstName" name="mdl.PersonalInformation.FirstName" value="" class="form-control" >
                </div>
            </div>
            <div class="row text-center py-3 mt-3">
                <div class="col-4 mx-auto">
                <input type="text" placeholder="LastName" name="mdl.PersonalInformation.LastName" value="" class="form-control" >
                </div>
            </div>
            <div class="row text-center py-3 mt-3">
                <div class="col-4 mx-auto">
                <input type="text" placeholder="PhoneNumber" name="mdl.PersonalInformation.PhoneNumber" value="" class="form-control" >
                </div>
            </div>
            <div class="row text-center py-3 mt-3">
                <div class="col-4 mx-auto">
                <input type="text" placeholder="Email" value="" name="mdl.PersonalInformation.Email" class="form-control" >
                </div>
            </div>
            <div class="row text-center py-3 mt-3">
                <div class="col-4 mx-auto">
                    <label for="cars">Curent Cuntry:</label>
                    <select class="form-control" name="mdl.PersonalInformation.CurentCuntry">
                        @{
                            foreach(var item in Enum.GetValues(typeof(EnumCurentCuntry)).OfType<EnumCurentCuntry>().ToArray())
                            {
                            <option value="@((int)item)"> @item.ToString()</option>
                            }
                        }
                    </select>  
                </div>
            </div>
            <div class="row text-center py-3 mt-3">
                <div class="col-4 mx-auto">
                    <label for="cars">Migration Type:</label>
                    <select  class="form-control" name="mdl">
                    @{
                        foreach(var item in Enum.GetValues(typeof(EnumMigrationType)).OfType<EnumMigrationType>().ToArray())
                        {
                        <option value="@((int)item)"> @item.ToString()</option>
                        }
                    }
                    </select>  
                </div>
            </div>
            <div class="row text-center py-3 mt-3">
                <div class="col-4 mx-auto">
                    <label for="cars">TargetCuntry:</label>
                    <select  class="form-control" name="mdl.PersonalInformation.TargetCuntry">
                    @{
                        foreach(var item in Enum.GetValues(typeof(EnumTargetCuntry)).OfType<EnumTargetCuntry>().ToArray())
                        {
                        <option value="@((int)item)"> @item.ToString()</option>
                        }
                    }
                    </select>  
                </div>
            </div>
            <div class="row text-center py-3 mt-3">
                <div class="col-4 mx-auto">
                    <label for="cars">Field Of Study:</label>
                    <select  class="form-control" name="mdl">
                    @{
                        foreach(var item in Enum.GetValues(typeof(EnumFieldOfStudy)).OfType<EnumFieldOfStudy>().ToArray())
                        {
                        <option value="@((int)item)"> @item.ToString()</option>
                        }
                    }
                    </select>  
                </div>
            </div>
            <div class="row text-center py-3 mt-3">
                <div class="col-4 mx-auto">
                <input type="text" placeholder="Grade" name="mdl.PersonalInformation.Grade" value="" class="form-control" >
                </div>
            </div>
            <div class="row text-center py-3 mt-3">
                <div class="col-4 mx-auto">
                    <label for="cars">University:</label>

                    <select  class="form-control" name="mdl">
                    @{
                        foreach(var item in Enum.GetValues(typeof(EnumUniversity)).OfType<EnumUniversity>().ToArray())
                        {
                        <option value="@((int)item)"> @item.ToString()</option>
                        }
                    }
                    </select>  
                </div>
            </div>
            <div class="row text-center py-3 mt-3">
                <div class="col-4 mx-auto">
                <input type="text" placeholder="Last Grade Point Average" name="mdl" value="" class="form-control" >
                </div>
            </div>
            <div class="row text-center py-3 mt-3">
                <div class="col-4 mx-auto">
                    <label for="cars">Curent City:</label>
                    <select  class="form-control" name="mdl.PersonalInformation.CurentCity">
                    @{
                        foreach(var item in Enum.GetValues(typeof(EnumCurentCity)).OfType<EnumCurentCity>().ToArray())
                        {
                        <option value="@((int)item)"> @item.ToString()</option>
                        }
                    }
                    </select>  
                </div>
            </div>
            <div class="row text-center py-3 mt-3">
                <div class="col-4 mx-auto">
                    <label for="cars">First Job:</label>
                    <select  class="form-control" name="mdl.PersonalInformation.FirstJob">
                    @{
                        foreach(var item in Enum.GetValues(typeof(EnumJobType)).OfType<EnumJobType>().ToArray())
                        {
                        <option value="@((int)item)"> @item.ToString()</option>
                        }
                    }
                    </select>  
                </div>
            </div>
            <div class="row text-center py-3 mt-3">
                <div class="col-4 mx-auto">
                    <label for="cars">SecondJob:</label>
                    <select  class="form-control" name="mdl.PersonalInformation.SecondJob">
                    @{
                        foreach(var item in Enum.GetValues(typeof(EnumJobType)).OfType<EnumJobType>().ToArray())
                        {
                        <option value="@((int)item)"> @item.ToString()</option>
                        }
                    }
                    </select>  
                </div>
            </div>                
            <div class="row text-center py-3 mt-3">
                <div class="col-4 mx-auto">
                    <label for="cars">WorkExperience:</label>
                    <select  class="form-control" name="mdl.PersonalInformation.WorkExperience">
                    @{
                        foreach(var item in Enum.GetValues(typeof(EnumWorkExperience)).OfType<EnumWorkExperience>().ToArray())
                        {
                        <option value="@((int)item)"> @item.ToString()</option>
                        }
                    }
                    </select>  
                </div>
            </div>
            <div class="row text-center py-3 mt-3">
                <div class="col-4 mx-auto">
                <input type="text" placeholder="Financial Situation" name="mdl.PersonalInformation.FinancialSituation" value="" class="form-control" >
                </div>
            </div>
            <h4 class="text-gradient text-primary mb-0">physical health:</h4>
                <h4 class="mb-4">Health system for patients (special):</h4>
                    <div class="row text-center py-3 mt-3">
                        <div class="col-4 mx-auto">
                            <label for="cars">Do you have a specific illness? Name it</label>
                            <select  class="form-control" name="mdl.PhysicalHealth.HealthSystemForPatients.DoYouHaveSpecificIllness">
                            @{
                                foreach(var item in Enum.GetValues(typeof(EnumDoYouHaveSpecificIllness)).OfType<EnumDoYouHaveSpecificIllness>().ToArray())
                                {
                                <option value="@((int)item)"> @item.ToString()</option>
                                }
                            }
                            </select>  
                        </div>
                    </div>
                <h4 class="mb-4">Applicant's food lifestyle:</h4>
                    <div class="row text-center py-3 mt-3">
                        <div class="col-4 mx-auto">
                            <label for="cars">Options:</label>
                            <select  class="form-control"   name="mdl.PhysicalHealth.ApplicantsFoodLifestyle.Option">
                            @{
                                foreach(var item in Enum.GetValues(typeof(EnumApplicantsFoodLifestyle)).OfType<EnumApplicantsFoodLifestyle>().ToArray())
                                {
                                <option value="@((int)item)"> @item.ToString()</option>
                                }
                            }
                            </select>  
                        </div>
                    </div>
                <h4 class="mb-4">Need to access a doctor:</h4>
                    <div class="row text-center py-3 mt-3">
                        <div class="col-4 mx-auto">
                            <label for="cars">How many times a year do you or your family usually see a doctor? For what reasons?</label>
                            <select  class="form-control"   name="mdl.PhysicalHealth.NeedToAccessDoctor.HowManyTimesYearDoYouOrYourFamilyUsuallySeeDoctor">
                            @{
                                foreach(var item in Enum.GetValues(typeof(EnumHowManyTimesYearDoYouOrYourFamilyUsuallySeeDoctor)).OfType<EnumHowManyTimesYearDoYouOrYourFamilyUsuallySeeDoctor>().ToArray())
                                {
                                <option value="@((int)item)"> @item.ToString()</option>
                                }
                            }
                            </select>  
                        </div>
                    </div>
                <h4 class="mb-4">The applicant expects the ratio of health costs to savings:</h4>
                    <div class="row text-center py-3 mt-3">
                        <div class="col-4 mx-auto">
                            <label for="cars">How much do you expect to spend on your treatment and medical issues during the year?</label>
                            <input type="text" placeholder="" value="" name="mdl.PhysicalHealth.HealthCostsToSavings.SpendsOnTreatment" class="form-control" required>
                        </div>
                    </div>
            <h4 class="text-gradient text-primary mb-0">Mental and emotional health:</h4>
                <h4 class="mb-4">Probability of dealing with mental illness:</h4>
                    <div class="row text-center py-3 mt-3">
                        <div class="col-4 mx-auto">
                            <label for="cars">Which of the following disorders do you have a history of? (Depression, anxiety, ...)</label>
                            <select  class="form-control"  name="mdl.MentalHealth.ProbabilityOfDealingWithMentalIllness.WhichOfTheFollowingDisordersDoYouHaveHistoryOf">
                            @{
                                foreach(var item in Enum.GetValues(typeof(EnumWhichOfTheFollowingDisordersDoYouHaveHistoryOf)).OfType<EnumWhichOfTheFollowingDisordersDoYouHaveHistoryOf>().ToArray())
                                {
                                <option value="@((int)item)"> @item.ToString()</option>
                                }
                            }
                            </select>  
                        </div>
                    </div>
                <h4 class="mb-4">Visit family and friends:</h4>
                    <div class="row text-center py-3 mt-3">
                        <div class="col-4 mx-auto">
                            <label for="cars">How many times a week do you visit your family?</label>
                            <select  class="form-control"   name="mdl.MentalHealth.VisitFamilyAndFriends.HowManyTimesWeekDoYouVisitYourFamily">
                            @{
                                foreach(var item in Enum.GetValues(typeof(EnumHowManyTimesWeekDoYouVisitYourFamily)).OfType<EnumHowManyTimesWeekDoYouVisitYourFamily>().ToArray())
                                {
                                <option value="@((int)item)"> @item.ToString()</option>
                                }
                            }
                            </select>  
                        </div>
                    </div>
          </div>
        </div>
        <div class="col-lg-5 col-md-12 ms-auto order-1 order-md-1 order-lg-1">
          <div class="p-3 pt-0">
            <div class="icon icon-shape bg-gradient-primary rounded-circle shadow text-center mb-4">
              <i class="ni ni-building"></i>
            </div>
            <h4 class="text-gradient text-primary mb-0">Welfare | Economy:</h4>
                <h4 class="mb-4">saving:</h4>
                    <div class="row text-center py-3 mt-3">
                        <div class="col-4 mx-auto">
                            <label for="cars">How much do you expect to save annually?</label>
                            <input type="text" placeholder="" value="" name="mdl.Welfare.Saving.HowMuchDoYouExpectToSaveAnnually" class="form-control"  required>
                        </div>
                    </div>
                    <div class="row text-center py-3 mt-3">
                        <div class="col-4 mx-auto">
                            <label for="cars">How much is your current savings?</label>
                            <input type="text" placeholder="" value="" name="mdl.Welfare.Saving.HowMuchIsYourCurrentSavings" class="form-control"  required>
                        </div>
                    </div>
                <h4 class="mb-4">specialty:</h4>
                    <div class="row text-center py-3 mt-3">
                        <div class="col-4 mx-auto">
                            <label for="cars">What is your job or skill now?</label>
                            <select  class="form-control"   name="mdl.Welfare.Specialty.WhatIsYourJobOrSkillNow">
                            @{
                                foreach(var item in Enum.GetValues(typeof(EnumWhatIsYourJobOrSkillNow)).OfType<EnumWhatIsYourJobOrSkillNow>().ToArray())
                                {
                                <option value="@((int)item)"> @item.ToString()</option>
                                }
                            }
                            </select>  
                        </div>
                    </div>
                    <div class="row text-center py-3 mt-3">
                        <div class="col-4 mx-auto">
                            <label for="cars">What is your second job or skill now?</label>
                            <select  class="form-control"   name="mdl.Welfare.Specialty.WhatIsYourSecondJobOrSkillNow">
                            @{
                                foreach(var item in Enum.GetValues(typeof(EnumWhatIsYourJobOrSkillNow)).OfType<EnumWhatIsYourJobOrSkillNow>().ToArray())
                                {
                                <option value="@((int)item)"> @item.ToString()</option>
                                }
                            }
                            </select>  
                        </div>
                    </div>
                    <div class="row text-center py-3 mt-3">
                        <div class="col-4 mx-auto">
                            <label for="cars">What job do you want to pursue after emigrating?</label>
                            <select  class="form-control"   name="mdl.Welfare.Specialty.WhatJobDoYouWantToPursueAfterEmigrating">
                            @{
                                foreach(var item in Enum.GetValues(typeof(EnumWhatJobDoYouWantToPursueAfterEmigrating)).OfType<EnumWhatJobDoYouWantToPursueAfterEmigrating>().ToArray())
                                {
                                <option value="@((int)item)"> @item.ToString()</option>
                                }
                            }
                            </select>  
                        </div>
                    </div>
            <h4 class="text-gradient text-primary mb-0">Cultural:</h4>
                <h4 class="mb-4">Religious - Value (Patriotism):</h4>
                    <div class="row text-center py-3 mt-3">
                        <div class="col-4 mx-auto">
                            <label for="cars">How committed are you to religious issues?</label>
                            <select  class="form-control"   name="mdl.Cultural.Religious.HowCommittedAreYouToReligiousIssues">
                            @{
                                foreach(var item in Enum.GetValues(typeof(EnumHowCommittedAreYouToReligiousIssues)).OfType<EnumHowCommittedAreYouToReligiousIssues>().ToArray())
                                {
                                <option value="@((int)item)"> @item.ToString()</option>
                                }
                            }
                            </select>  
                        </div>
                    </div>
                    <div class="row text-center py-3 mt-3">
                        <div class="col-4 mx-auto">
                            <label for="cars">How important is the future of Iran for you?</label>
                            <select  class="form-control"   name="mdl.Cultural.Religious.HowImportantIsTheFutureOfIranForYou">
                            @{
                                foreach(var item in Enum.GetValues(typeof(EnumHowImportantIsTheFutureOfIranForYou)).OfType<EnumHowImportantIsTheFutureOfIranForYou>().ToArray())
                                {
                                <option value="@((int)item)"> @item.ToString()</option>
                                }
                            }
                            </select>  
                        </div>
                    </div>
                <h4 class="mb-4">Immigration:</h4>
                    <div class="row text-center py-3 mt-3">
                        <div class="col-4 mx-auto">
                            <label for="cars">How important is the immigration of the destination community to you?</label>
                            <select  class="form-control"   name="mdl.Cultural.Immigration.ImmigrationOfTheDestination">
                            @{
                                foreach(var item in Enum.GetValues(typeof(EnumHowImportantIsTheImmigrationOfTheDestinationCommunityToYou)).OfType<EnumHowImportantIsTheImmigrationOfTheDestinationCommunityToYou>().ToArray())
                                {
                                <option value="@((int)item)"> @item.ToString()</option>
                                }
                            }
                            </select>  
                        </div>
                    </div>
                <h4 class="mb-4">Social relationship matching:</h4>
                    <div class="row text-center py-3 mt-3">
                        <div class="col-4 mx-auto">
                            <label for="cars">
                                How important is the similarity of the culture and behavior of the destination community to the community of origin?
                                </label>
                            <select  class="form-control"   name="mdl.Cultural.SocialRelationshipMatching.EnumSimilarityOfTheCulture">
                            @{
                                foreach(var item in Enum.GetValues(typeof(EnumSimilarityOfTheCulture)).OfType<EnumSimilarityOfTheCulture>().ToArray())
                                {
                                <option value="@((int)item)"> @item.ToString()</option>
                                }
                            }
                            </select>  
                        </div>
                    </div>
                <h4 class="mb-4">Language:</h4>
                    <div class="row text-center py-3 mt-3">
                        <div class="col-4 mx-auto">
                            <label for="cars">First Language Level?(English)</label>
                            <select  class="form-control" name="mdl.Language.FirstLanguageLevel">
                            @{
                                foreach(var item in Enum.GetValues(typeof(EnumLanguageLevel)).OfType<EnumLanguageLevel>().ToArray())
                                {
                                <option value="@((int)item)"> @item.ToString()</option>
                                }
                            }
                            </select>  
                        </div>
                    </div>
                    <div class="row text-center py-3 mt-3">
                        <div class="col-4 mx-auto">
                            <label for="cars">Second Language Level?(French)</label>
                            <select  class="form-control" name="mdl.Language.SecondLanguageLevel">
                            @{
                                foreach(var item in Enum.GetValues(typeof(EnumLanguageLevel)).OfType<EnumLanguageLevel>().ToArray())
                                {
                                <option value="@((int)item)"> @item.ToString()</option>
                                }
                            }
                            </select>  
                        </div>
                    </div>
            <h4 class="text-gradient text-primary mb-0">environmental:</h4>
                <h4 class="mb-4">Temperature and weather:</h4>
                    <div class="row text-center py-3 mt-3">
                        <div class="col-4 mx-auto">
                            <label for="cars">What is your unfavorable weather?</label>
                            <select  class="form-control"   name="mdl.Environmental.TemperatureAndWeather.WhatIsYourUnfavorableWeather">
                            @{
                                foreach(var item in Enum.GetValues(typeof(EnumWhatIsYourUnfavorableWeather)).OfType<EnumWhatIsYourUnfavorableWeather>().ToArray())
                                {
                                <option value="@((int)item)"> @item.ToString()</option>
                                }
                            }
                            </select>  
                        </div>
                    </div>
                <h4 class="mb-4">Climate:</h4>
                    <div class="row text-center py-3 mt-3">
                        <div class="col-4 mx-auto">
                            <label for="cars">Problems with day and night hours?</label>
                            <select  class="form-control"   name="mdl.Environmental.Climate.ProblemsWithDayAndNightHours">
                            @{
                                foreach(var item in Enum.GetValues(typeof(EnumProblemsWithDayAndNightHours)).OfType<EnumProblemsWithDayAndNightHours>().ToArray())
                                {
                                <option value="@((int)item)"> @item.ToString()</option>
                                }
                            }
                            </select>  
                        </div>
                    </div>
            <h4 class="text-gradient text-primary mb-0">educational:</h4>
                <h4 class="mb-4">Tuition:</h4>
                    <div class="row text-center py-3 mt-3">
                        <div class="col-4 mx-auto">
                            <label for="cars">If you want to go to school, how much can you pay per year?</label>
                            <input type="text" placeholder="" value="" name="mdl.Educational.Tuition.HowMuchCanYouPayPerYear" class="form-control" required >
                        </div>
                    </div>
                <h4 class="mb-4">Probability of acceptance:</h4>
                    <div class="row text-center py-3 mt-3">
                        <div class="col-4 mx-auto">
                            <label for="cars">Academic resume (we calculate with the sheikh)</label>
                            <select  class="form-control"   name="mdl.Educational.ProbabilityOfAcceptance.AcademicResume">
                            @{
                                foreach(var item in Enum.GetValues(typeof(EnumAcademicResume)).OfType<EnumAcademicResume>().ToArray())
                                {
                                <option value="@((int)item)"> @item.ToString()</option>
                                }
                            }
                            </select>  
                        </div>
                    </div>
          </div>
        </div>
      </div>
    </div>
    <div class="col-md-7 mx-auto text-center">
        <button type="submit" class="btn btn-primary btn-lg mb-3 mb-sm-0">Submit the data</button>
    </div>
 </form>
</section>
*@

@*  <div style="margin: 50px;
    box-shadow: inset 0 0 15px 0px #009688;
    border-radius: 20px;
    padding: 5px 20px 5px 30px;">
    <hr/>
    <h5>@ViewData["Cultural"] </h5>
    <h5>@ViewData["PhysicalHealth"]</h5>
    <h5>@ViewData["MentalHealth"] </h5>
    <h5>@ViewData["Environmental"] </h5>
    <h5>@ViewData["Educational"] </h5>
    <h5>@ViewData["Welfare"] </h5>
    <br/>
    <h5>@ViewData["Result"]</h5>
    <h5>@ViewData["Percentage"]</h5>
    <h5>@ViewData["Message"]</h5>
    <hr/>
  </div>
*@
